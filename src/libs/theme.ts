import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    button: {
      fontSize: 14,
      textTransform: "none",
      fontWeight: 700,
    },
  },
});
export default theme;
